package ba.sake.hepek

package object implicits extends HepekAliases {

  val StaticSiteSettings = html.statik.StaticSiteSettings
  type StaticSiteSettings = html.statik.StaticSiteSettings

  val BlogSettings = html.statik.BlogSettings
  type BlogSettings = html.statik.BlogSettings

  val Section = html.statik.Section
  type Section = html.statik.Section

}
